//MIDTERM PSEUDO CODE
function setup() {
createCanvas(900, 900);
//background() beige
}

function draw() {
//opacity needs to slowly develop then add more cracks?

}

if(mousePressed()){
  ellipse(mouseX, mouseY, 10, 10);
}
